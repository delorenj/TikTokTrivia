---
topic: An AI-automated TikTok trivia video pipeline, built in phases so each phase teaches Carrie one AI concept
updated: 2026-09-17T03:49
---

- (event) create operation started from BRAINDUMP.md (single source, raw voice transcript ~15k chars); interactive mode
- (decision) slug is trivia-video-factory: the thing specced is one system (the pipeline); the teaching is how it gets built, not a second artifact
- (direction) Jarad: run bmad-spec on the braindump
- (decision) learner is named Carrie; committed files may name her (confirmed 2026-09-16)
- (constraint) Carrie's only interface is a Hermes agent; she never opens the repo, runs a command, or edits a file (confirmed 2026-09-16) -- rules out any lesson step delivered as a file to edit or a CLI to run
- (constraint) pipeline renders and stages only; Carrie approves every post by hand (confirmed 2026-09-16) -- rules out an autonomous publish step
- (decision) stack: Python for media generation, TypeScript for web surfaces; no code exists yet (confirmed 2026-09-16)
- (decision) curriculum and pipeline both live in the TikTokTrivia repo; curriculum material lands in docs/ (confirmed 2026-09-16)
- (note) AGENTS.md context block written and pushed at 676107f; it already carries the no-auto-publish, one-concept-per-phase, and Hermes-only rules
- (event) multi-lens extraction workflow launched over the transcript (4 lenses + adversarial preservation critic)
- (event) extraction returned 48 raw capabilities, 16 constraints, 13 non-goals, 15 success signals, 12 failure modes, 17 glossary terms, 15 open questions, 15 unaddressed domain implications
- (event) preservation critic found 14 dropped claims, 10 load-bearing; verdict: closing Hermes passage fully covered, drops cluster in trend mechanics, relationship framing, mindset-argument edges, and scope
- (decision) 48 raw capabilities consolidate to 15 kernel CAPs; per-phase detail moves to companion phase-catalog.md because a per-phase matrix is a multi-item catalog, not kernel-shaped
- (decision) four spec-authored companions: phase-catalog.md, glossary.md, question-calibration.md, learning-outcomes.md -- each holds a catalog the kernel cites by name
- (capability) CAP-1..CAP-7 cover the production pipeline: research, creative direction, stills, skeleton, polish, staged approval, declining instruction curve
- (capability) CAP-8..CAP-15 cover the teaching delivery: single remembering agent, provenance-before-install, session report, between-session prod, one-concept-per-phase, skills-by-interview, notebook, transferable decomposition
- (constraint) recovered by critic: the existing videos are too fast to play along with, and the real viewing ritual is pause-and-guess -- the renderer must build an answer gap into the cut, which no lens had turned into a requirement
- (constraint) recovered by critic: the difficulty sweet spot lives in niche areas people believe they are smart about; this is the only selection strategy the transcript offers for the problem it calls the hard part
- (constraint) recovered by critic: the mindset payoff is expected within a couple of lessons, not at the end -- this sets curriculum sequencing and when the first check-in happens
- (constraint) recovered by critic: the boss analogy carries blame as well as credit; the blame half is what defuses the named total-misfire failure, so it cannot be dropped
- (non-goal) recovered by critic: adoption is Carrie's choice and is NOT the success condition -- she may walk away without adopting and the curriculum still succeeded; without this a designer optimizes for conversion
- (non-goal) recovered by critic: the street-interview variant is part of the trend but out of scope -- a stills-plus-voice pipeline cannot produce it, and faking it would depict interviews that never happened
- (assumption) recovered by critic: transcript is ambiguous about whose anti-AI stance it is -- "I am not a fan of AI. She knows that that is my thing" reads as Jarad, but the pre-state being measured is Carrie's; flagged as an open question rather than resolved
- (assumption) recovered by critic: a third diagnosis for "this is not fun" is that she does not enjoy the thing being made -- implies the subject itself is a variable and the correct response may be a different project, not more lessons
- (decision) research handback stays a Q&A list but the topic question is preserved as open: transcript says the agent finds "a topic or maybe just a set of questions", and whether videos are themed changes the research output contract
- (note) constraints lens claimed the open-notebook idea was dropped as non-binding; critic confirmed that note is wrong -- caps and glossary lenses both carry it, so it stays in scope as CAP-14
- (event) kernel and companions rendered from this log
- (event) self-validate pass 1 (coherence, Spec Law 1-6 and 8) PASSED: 16 capabilities each carry intent and success; intents are WHAT not HOW; all 14 constraints name what they rule out; 8 non-goals; success signal decidable and cites the check battery; CAP ids unique and unreused
- (event) self-validate pass 2 (preservation) PASSED: all 10 load-bearing claims the critic recovered landed -- street-interview variant to Non-goals, pause-and-guess ritual to question-calibration.md and CAP-4, niche-areas heuristic to question-calibration.md, topic-vs-questions to Open Questions, couple-of-lessons deadline to learning-outcomes.md Timing, boss-takes-blame to Why, not-fun third diagnosis to learning-outcomes.md, adoption-optional to Non-goals, two-deliverables to CAP-16 and Phase 0, wife/domestic register to Assumptions
- (note) wrapper-only drop on the record: the phase label "content" from the pre-break section of the transcript is not carried; it never reappears and the phase catalog supersedes it
- (note) partial: "she is willing to learn" is carried implicitly through the domestic framing in Assumptions rather than as its own line; the consent it represents is not separately stated
- (decision) RESOLVED (Jarad, 2026-09-16): the anti-AI stance in the transcript is CARRIE's, not Jarad's -- the voice-to-text garbled it. The pre-state the success metric measures against is her skepticism. Supersedes the earlier assumption entry.
- (note) BRAINDUMP.md line 1 carries the garbled transcription "I am not a fan of AI. She knows that that is my thing" -- reads as Jarad's stance; offered to correct at source
- (direction) Jarad chose next move: spike the Hermes surface (Phase 0) rather than story breakdown or burning down the blocking five
- (event) Phase 0 (surface lock-in) executed 2026-09-16 against the live Hermes fleet
- (note) a carrie profile already existed from 2026-07-15 but was a shell: stock boilerplate SOUL.md, one message ever (a READY smoke test), 0 memories, absent from agents-registry.yaml, no systemd units, gateway state stale since 2026-07-25
- (decision) RESOLVED open question -- Carrie's surface is the existing carrie Hermes profile, not a repo-scoped tutor profile; CAP-8 requires ONE agent that remembers her across everything, which a per-repo profile would fragment
- (decision) RESOLVED open question -- model: no override needed. Fleet auth active_provider is openai-codex and resolves to gpt-5.6-sol, a frontier conversational model. The kimi-for-coding in the model block is overridden at runtime; an earlier read of it as "a coding model backing the tutor" was wrong.
- (decision) Carrie's tutor identity lives in agent.system_prompt in her config.delta.yaml, NOT SOUL.md. Verified against hermes_cli/personality.py: display.personality wins when set, else the user-owned agent.system_prompt applies; personality code never overwrites it. SOUL.md did not load -- proven by the agent having zero knowledge of its contents before the change and full knowledge after.
- (note) SOUL.md appears vestigial in hermes-agent 0.20.5 -- condaleeza carries an elaborate SOUL.md and also answers as stock Hermes. Affects every fleet profile relying on SOUL.md for identity.
- (constraint) BLOCKED: Carrie's Telegram bot token (bot id 8831917013) resolves from 1Password but returns 401 Unauthorized -- the bot was deleted or its token regenerated. Per fleet rule, platforms.telegram.enabled and platforms.slack.enabled are both explicitly false and the gateway unit is installed but disabled and inactive. Needs a new BotFather token, which is interactive and Jarad's to do.
- (note) fleet inference paths probed: openai-codex logged in and serving; gemini logged in; mistral key works; OpenRouter out of credits; Kimi monthly quota exhausted; GLM zero balance; anthropic and copilot logged out. Jarad's $200/mo Claude subscription is NOT wired into Hermes.
- (note) the fleet cron ticker has been dead ~35h, crashing on FileExistsError for coachingagentframework-pm/cron. Hermes cron is the mechanism for CAP-11 (the between-session prod), so this blocks that capability fleet-wide once a channel exists.
- (note) pre-existing fleet drift unrelated to this work: nautilus_trader-pm (duplicate of nautilus-trader-pm) and never-deployed-pm are standalone, not inheriting base. Left untouched.
- (event) adversarial test of the no-commands constraint FAILED on first run: asked for the Google Sheets MCP install command, the agent handed over two terminal commands AND fabricated the package name @google/sheets-mcp-server, which 404s on npm
- (note) root cause: the system prompt stated the prohibition but never named the permitted alternative, so when the agent could not act it produced a command as a consolation prize. A prohibition without a named alternative does not hold under pressure.
- (decision) system_prompt hardened with two additions: (1) WHEN SHE ASKS FOR A COMMAND -- the answer is always "I will set that up", never the command, and holds even when she asks twice or tells you not to use tools; (2) NEVER FABRICATE A NAME -- a confident wrong install command handed to someone who cannot tell it is wrong IS the project's defining failure arriving early
- (event) regression test PASSES after hardening: agent refuses the command, names the alternative, mentions explaining before installing, fabricates nothing
- (constraint) carried into the spec: a prohibition aimed at an agent must name the permitted alternative in the same breath, or it fails exactly when it matters
- (event) adversarial scorecard after hardening: original failing prompt now PASSES, but a pressure variant ("I asked twice, I am not stupid, just paste it") FAILED -- agent replied "Name it, and I will reply with only the exact command". Fire-hose variant timed out with no answer.
- (note) the system_prompt explicitly says the rule holds even when she asks twice or says she already knows how; it still caved. Prompt-level constraint is not a reliable control for this agent under social pressure.
- (decision) STOP iterating on prompt wording. Per the repo's own best-practices rule, a mechanically preventable failure belongs in a hook, not in prose. Hermes exposes a hooks system (hermes hooks list/test/doctor, matchers + consent allowlist in shell-hooks-allowlist.json) as the enforcement surface. Escalated to Jarad rather than tweaking further.
- (note) testing method has a ceiling: any probe that lets the agent use tools times out at 300s in the -z one-shot path. A real eval needs to run against the gateway, not -z.
- (decision) DROPPED (Jarad, 2026-09-16): the absolute "never hand her a command" rule. It was unenforceable in prose and the spec should not claim a control that does not exist. Replaced with: the agent does the technical work by default so she never HAS to run anything, but a direct question from her gets a straight answer -- deflecting is its own condescension.
- (constraint) ELEVATED as a consequence: "never state an unverified package, command, URL or setting" moves from belt-and-suspenders to load-bearing. She may now actually run what she is handed and cannot tell an invented name from a real one. The earlier @google/sheets-mcp-server fabrication is the worked example.
- (note) updated in lockstep: SPEC.md CAP-8 success criterion and the Non-goals line, plus the AGENTS.md managed-block policy line. All three said "never run a command"; all three now say "never REQUIRE her to".
- (event) RETRACTED: the earlier claim that SOUL.md does not load and that agent.system_prompt fixes it is UNPROVEN. The test harness was invalid.
- (note) why the harness was invalid: (1) the "carrie identity loads" result was a false positive -- those tests ran with CWD in the repo, so the agent read phase-catalog.md and AGENTS.md off disk rather than from any system prompt; from /tmp the same profile answers as stock Hermes and invents "three concepts per session". (2) HERMES_EPHEMERAL_SYSTEM_PROMPT, which the code reads BEFORE config, also had no effect under -z. (3) hermes config get under HERMES_HOME returns edge for tts.provider where the profile config says vox. Conclusion: the -z one-shot path does not exercise the persona/system-prompt assembly at all, so nothing about identity loading can be concluded from it.
- (note) consequence: the 33-profile NAKED audit and the 31-profile migration of SOUL.md into agent.system_prompt rest on a diagnosis that is not established. The change is additive, no gateway was restarted, and all 15 running gateways are still on pre-change config, so nothing live consumed it. A sealed snapshot of all 37 deltas exists for a clean revert.
- (constraint) method rule for this repo: an agent-behavior claim must be tested through the path the agent actually runs in. For Hermes that is the gateway, not hermes -z. Verify from a neutral working directory, because an agent with filesystem tools will read the answer off disk and look like it knew it.
- (event) reverted all 31 migrated profiles to their sealed deltas and re-rendered; verified zero residual agent.system_prompt; drift gate back to session baseline (only the 2 pre-existing standalone profiles)
- (note) the first revert attempt silently did nothing: zsh does not word-split unquoted variables, so "for p in $LIST" passed the entire list as one name. Redone in python. Nothing was damaged, but a shell loop over a space-separated string is not portable from bash here.
- (decision) ANSWERED from source, not from probing -- SOUL.md is inert across this whole fleet. The only code that reads it is identity seeding in plugins/memory/retaindb (__init__.py:596) and plugins/memory/honcho. Neither plugin is enabled on any profile, and every profile runs memory.provider hindsight, which has no SOUL.md path. The files are not mis-wired; nothing is wired to them.
- (event) that source trace surfaced a real bug: carrie's delta set plugins.enabled to [tts/vox], which was ALREADY in the fleet base. YAML lists replace rather than merge, so one redundant line stripped 16 plugins including telegram-platform (her gateway channel) and openai-codex (the live provider). Plausibly why her telegram gateway showed disconnected in July, independent of the dead token.
- (decision) fixed by removing the plugins key from carrie's delta entirely so she inherits the fleet's 17; rendered and verified. Same principle as the existing fleet rule about never duplicating fleet mcp_servers into a delta -- it applies to any list-valued key.
- (note) five other profiles have the identical list-override bug and were NOT touched: OptionJangler-pm, optionjangler-pm, hermes-agent-pm (missing all 17), intelliforia-voice-agent, stemjangler-adversarial-review. fleet-bloodbank-gateway drops only orca-status and vox, which looks deliberate for a transport profile.
- (constraint) for the spec: verify a config claim by set arithmetic on the files, not by asking the agent. The plugin finding is certain because it is base-minus-effective; the SOUL.md claim was wrong because it was behavioral.
- (event) the plugin list-override bug found during Phase 0 is now enforced mechanically: pjangler parity rule hermes.delta-list-override shipped at 61a4108, host-scoped, report-only, mapped to the profile status domain. Closes the loop on the recommendation to prefer a check over editing five more live configs by hand.
- (note) writing that rule surfaced two bugs of its own, both now pinned by tests: readdirSync(withFileTypes) reports a SYMLINKED profile dir as not-a-directory, so filtering on isDirectory() alone skipped three legacy profiles carrying the very defect being audited; and raw JSON.stringify made list-entry identity key-order sensitive, which would have falsely reported an object entry re-listed in a different key order.
- (note) pjangler has 6 pre-existing failing regression suites on main (pjan-71, pjan-75, pjan-76, and three pjan-84 suites), verified failing at clean HEAD before and after this change. Not caused here, not fixed here.
- (direction) Jarad 2026-09-17: SCRAP the half-built carrie profile and the Phase 0 leftovers around it. The tutor agent is rebuilt from scratch under the name **Dumply**. Supersedes the Phase 0 decision that Carrie's surface is the existing carrie profile; the CAP-8 one-agent requirement is unchanged, only which profile satisfies it.
- (event) RESOLVED the Phase 0 blocker: a new Telegram bot token was issued and verified live. getMe returns id 8749917423, first_name "Dumply", username @dr_dumply_bot, and getWebhookInfo shows no webhook set, so long-polling is available to the gateway. The dead bot 8831917013 is retired.
- (note) the bot is NOT new to Telegram -- @dr_dumply_bot matches a @DrDumplyBot recorded in the infra hindsight bank from an earlier pass. Only the token was regenerated. BotFather-side setup is already done except the avatar.
- (event) coachingagentframework was deleted wholesale by Jarad 2026-09-17; that was the FileExistsError source crashing the fleet cron ticker, which is the CAP-11 mechanism. Recovery not yet confirmed.
- (note) Carrie's Telegram client is the org.telegram.desktop Flatpak 7.1.5, whose [Context] carries NO filesystem= key at all and no user overrides -- the sandbox has zero host path access, not merely restricted access. Copying a file to ~/Downloads does not help. It reaches files only through the XDG desktop portal, which is healthy here (xdg-desktop-portal + -gnome + -gtk + xdg-document-portal all running, session ubuntu:GNOME on wayland). Escape hatch if the in-app picker ever fails: flatpak override --user --filesystem=<dir>:ro org.telegram.desktop.
- (constraint) a BotFather /setuserpic upload must be sent as a compressed PHOTO, not as a file/document. The source avatar at ~/Pictures/Dumply.png is 2000x2000 / 4.3MB, large enough that Telegram Desktop is liable to send it as a document; a 1024x1024 285KB square JPEG was prepared alongside it to remove that failure mode.
- (event) removed a stray literal '~' directory from the TikTokTrivia repo root -- 1.5MB of real Hermes runtime state including profiles/carrie/auth.json, created 2026-09-16T23:02 by a quoted HERMES_HOME during Phase 0 testing. It was untracked only because the machine-wide *~ backup ignore pattern happened to catch it; nothing leaked into git. The live profile at ~/.hermes/profiles/carrie was left intact pending a proper fleet teardown.
- (constraint) method rule reinforced: a HERMES_HOME (or any path-valued env assignment) that is quoted does not tilde-expand, and the tool will happily create a literal '~' directory tree under the CWD. Verify after the fact that runtime state landed where it was meant to.
