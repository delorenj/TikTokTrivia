---
scope: The TikTokTrivia production pipeline: the seven phases from trend research through stage-and-approve, plus the agent surface (Dumply) that runs them for Carrie.
purpose: build-substrate
altitude: initiative
updated: 2026-09-17T19:11
---

- (direction) Jarad chose the Coaching path and spine-only as the deliverable (2026-09-17). No HTML rendering, no cost model, no docs/ curriculum plan in this run.
- (constraint) INHERITED from SPEC-trivia-video-factory, binding and not re-decided here: Carrie's only interface is the Dumply Hermes agent (CAP-8); nothing publishes to TikTok without her explicit approval (CAP-6); every stage is defined by the artifact it hands back; each stage carries an explicit model assignment justified by difficulty; end-to-end walking skeleton before any phase is deepened (CAP-4); one new concept per phase (CAP-12).
- (direction) Jarad REJECTED the ports-and-adapters framing as over-engineering (2026-09-17). There is NO application codebase and NO pipeline to build. Dumply is a plain high-capability agent in Telegram with full read/write/computer access to a machine. Hermes is chosen only for the friendly interface (Telegram) and for inference/model/proxy control -- it is not an application platform. The entire opinionated product is an agent file plus skills, including one skill that teaches making the video. BMAD runs under the hood to keep the plan on rails. Ad-hoc iteration ('you don't like it? what don't you like?') IS the lesson, and a fixed pipeline would obstruct it.
- (question) NEW, raised by Jarad: does Dumply run on Carrie's own machine rather than big-chungus? He says she already has a local Hermes gateway on her desktop and wants the agent to have full read/write/computer access to HER machine. The dumply profile provisioned today runs on big-chungus. This is now the load-bearing deployment decision.
- (decision) PARADIGM: agent-native, skill-driven. There is no application codebase, no pipeline, no ports/adapters. The product is a Hermes agent (Dumply) with a good agent file plus skills. Binds all capabilities. Prevents: a future builder erecting a Python pipeline or service layer between Carrie and the work, which would make ad-hoc iteration -- the actual lesson -- harder rather than easier.
- (decision) DEPLOYMENT: Dumply runs on big-chungus as the existing 'dumply' Hermes profile, NOT on carries-macbook-air. Verified: her Mac is live on the tailnet (100.81.162.91) but the remembered 'local Hermes gateway on her desktop' was actually hermes-carrie-backend.service on 100.66.29.76, which IS big-chungus. Nothing in making a video needs her laptop; the finished mp4 is delivered over Telegram. Prevents a macOS/launchd install outside the fleet.
- (decision) STILLS: Ideogram 3.0 via fal.ai (fal-ai/ideogram/v3), NOT Ideogram direct. Jarad's stated preference for fal, and one fal key reaches every image, image-to-image and video model on the platform -- which is what the 'let us try a different model' iteration beat needs without a new account per experiment. Key verified live 2026-09-17: POST fal.run/fal-ai/ideogram/v3 with an empty body returned 422 'Field required: prompt' (schema rejection, not auth), costing nothing. Vault item 'Fal' vdjnvpthqcqwsareu4zvj4dugq, field 'API Key'.
- (constraint) HARD TRAP: use Ideogram v3, never v4. Ideogram 4.0's generate endpoint DROPPED style_reference_images, style_codes, style_type, character_reference_images and seed -- it accepts only text_prompt, json_prompt, resolution, rendering_speed, enable_copyright_detection. The newer model is the wrong model here, because ten frames looking like one video is the hard requirement. Verified against developer.ideogram.ai/openapi.json.
- (decision) TTS: vox (https://vox.delo.sh), already registered as an MCP server in the dumply profile (mcp_servers: codegraph, pjangler, plane, vox). Zero new work, zero cost. Backed by openbmb/VoxCPM2, 2B params, Apache-2.0, with BOTH description-based voice design and reference cloning. Description mode -- a parenthetical at the start of the text with no voice field -- lets Carrie describe a narrator and hear it in seconds with no file to manage. ElevenLabs is already armed as vox's fallback, so a premium read is a config flip rather than a purchase.
- (constraint) vox /synthesize-url links expire after 3600s. They are a delivery convenience, not storage; narration bytes must be downloaded and written to durable storage before a render depends on them.
- (decision) ASSEMBLY: raw ffmpeg invoked as a subprocess, for both the walking skeleton and later polish. ffmpeg 7.1.1 is already installed with libass, libx264, libfreetype and libfontconfig. Research executed both the skeleton chain (10 stills, per-scene durations, concat, mux narration, 1080x1920 H.264/yuv420p/30fps + AAC 128k) and the polish chain (zoompan Ken Burns, burned-in ASS captions) on this machine; both exited 0. Adding polish lengthens the filtergraph and changes nothing else.
- (constraint) HARD TRAP: MoviePy is excluded. Last release 2.2.1 (2025-05-21), dormant -- documentation-only commits in 2026, 87 unreleased fixes on master. Its own changelog states TextClip is broken with Pillow > 11.2 while the released pin resolves to Pillow 11.3.0, inside the broken range, and TextClip is exactly what a captions step reaches for. ffmpeg-python is also excluded: no release in over seven years despite being the top search result.
- (decision) ARTIFACTS: generated stills, narration audio and rendered videos go to S3 at s3.delo.sh (the DeLoDrive MinIO; credentials in DeLoSecrets item 'DeLoDrive (MinIO)' hwatizsoschtnq5oleume6kdpu). Verified live: returns x-amz-request-id headers. They do NOT go in the TikTokTrivia repo, which holds only the agent file, skills and planning artifacts -- per the standing rule that repos hold source, never runtime droppings. Prevents binary bloat and keeps artifacts surviving a Dumply rebuild.
- (decision) BOARD: the plane MCP server is already wired into dumply. Carrie's work is tracked on a real Plane board, primarily so she sees how a board works -- an incidental lesson, not a dependency. The TikTokTrivia board does not exist yet: .project.json declares identifier TIKT with an empty board_id.
- (decision) DELIVERY: the finished mp4 goes to Carrie over Telegram. This is what makes running on big-chungus invisible to her and removes any need for her to fetch a file or open a second interface (CAP-8).
- (version) Verified current 2026-09-17: ffmpeg 7.1.1-1ubuntu4.2; Ideogram v3 via fal-ai/ideogram/v3 at 0.03 TURBO / 0.06 BALANCED / 0.09 QUALITY USD per image; vox backed by openbmb/VoxCPM2; MoviePy 2.2.1 (rejected); hermes-agent 0.20.5, runtime release 0408fec7a153e6c32c064acd2b8053917f1525f1.
- (note) Hygiene: the DeLoSecrets 'Fal' item stores its API Key as field type STRING rather than CONCEALED, so it prints in cleartext in op JSON output without --reveal. Same defect the retired DrDumply item had. Worth promoting to concealed.
- (question) BLOCKER for the first lesson: dumply's config has web.backend, web.search_backend and web.extract_backend all set to empty strings, so the agent has NO web search. The first phase's entire taught concept is 'it cannot answer this from memory, it has to go look'. A browser block IS configured (engine: auto) and x_search is pointed at grok-4.20-reasoning, so the capability may be reachable by another route, but plain web search is not wired. Must be resolved before Carrie's first session. Needs Jarad to pick a backend.
- (constraint) ANTI-FABRICATION is architectural here, not stylistic. Every externally-named thing in this spine was probed live rather than recalled: the fal key (422 schema rejection), ffmpeg's build flags, vox's OpenAPI, the Ideogram v3-vs-v4 parameter sets, s3.delo.sh's x-amz headers, and the MCP server list in the rendered profile. The project's defining failure is a confident wrong name reaching someone who cannot tell it is wrong, so any future amendment to this spine carries the same obligation to verify before naming.
